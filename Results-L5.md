# Эксперимент L5: сравнение трёх моделей DeepSeek на одной задаче

- Дата: 2026-09-06
- Ветка: `L5` (от `main`, tip `2c8f134`)
- Инструмент: сборка `Program.cs` из ветки `L5` (CLI-опции + флаг `--stats`), `dotnet build -c Release`
- Конфиг: `C:\Props\llm-cli.properties` (передаётся через `--config`; `api_key` в команду/репозиторий не попадает)
- Параметры фиксированы для всех трёх прогонов: `--temperature 0`, `--thinking disabled`, `stream=false`; одно `user`-сообщение без system-промпта
- Вопрос (одинаковый): **«Сорока летит, а собака на хвосте сидит. Может ли это быть?»**
- Метрики из ответа API (`usage`) и таймера запроса (`--stats`): полное время запроса, prompt/completion/total токены, выходные токены/с, стоимость.

## Цены (справочно)

Источник: https://api-docs.deepseek.com/quick_start/pricing (CNY за 1 млн токенов, вход без кэш-хита / выход).
Пиковый тариф (Пн–Пт, Пекин, 9–12 и 14–18) = ровно ×2 от непикового. 2026-09-06 — воскресенье, поэтому применим **непиковый** тариф.

| Модель | вход, ¥/1M (непик/пик) | выход, ¥/1M (непик/пик) |
|---|---|---|
| `deepseek-v4-flash` | 1.5 / 3.0 | 4.5 / 9.0 |
| `deepseek-v4-pro` | 4.5 / 9.0 | 13.5 / 27.0 |
| `deepseek-v4-flash-vision-exp` | 1.5 / 3.0 | 4.5 / 9.0 |

## Ответы моделей

### deepseek-v4-flash
> Это старая загадка-шутка. Ответ: **Да, может**.
>
> Разгадка проста: **собака сидит на собственном хвосте**. А сорока летит сама по себе. В условии не сказано, что собака сидит на хвосте у сороки.

### deepseek-v4-pro
> Да, может.
>
> **Объяснение:**
> Собака сидит на **своём собственном** хвосте (например, на земле), а сорока в это время летит. Фраза построена так, что кажется, будто собака сидит на хвосте сороки, но на самом деле хвост принадлежит самой собаке.

### deepseek-v4-flash-vision-exp
> Это классическая загадка на логику и внимательность.
>
> **Ответ: Да, может.**
>
> **Разгадка:**
> Сорока **летит** (находится в воздухе), а собака **сидит на хвосте** — но не у сороки, а **на своём собственном хвосте** (или на хвосте другой собаки, которая сидит на земле).
>
> В русском языке фраза «на хвосте сидит» часто означает «сидит на чьём-то хвосте», но здесь подразумевается, что собака просто сидит, а её хвост находится под ней. Сорока при этом летит отдельно.
>
> Если же понимать буквально, что собака сидит на хвосте летящей сороки, то это невозможно — сорока слишком мала, чтобы выдержать собаку. Но загадка построена на двусмысленности предлога «на».

## Метрики

| Модель | Время, с | prompt | completion | total | Выход, ток/с | Стоимость ¥ (непик) | Стоимость ¥ (пик) |
|---|---|---:|---:|---:|---:|---:|---:|
| `deepseek-v4-flash` | 1.34 | 26 | 67 | 93 | 50.0 | 0.000341 | 0.000681 |
| `deepseek-v4-pro` | 2.65 | 26 | 81 | 107 | 30.6 | 0.001211 | 0.002421 |
| `deepseek-v4-flash-vision-exp` | 2.37 | 26 | 210 | 236 | 88.8 | 0.000984 | 0.001968 |

## Сравнение

**Качество (все ответили верно):** разгадка у всех одна и та же — «да, собака сидит на собственном хвосте, а сорока летит отдельно; двусмысленность предлога "на"». Фактических ошибок нет ни у одной модели. Разница только в полноте: `pro` — самый сжатый и чистый ответ; `flash` — коротко и по делу (без лишнего); `vision-exp` — самый подробный, с явным разбором буквального (невозможного) прочтения, но заметно многословен.

**Скорость:** `flash` — самая быстрая (1.34 с), почти в 2 раза быстрее `pro` (2.65 с); `vision-exp` — между ними (2.37 с). По «выходной» скорости генерации лидирует `vision-exp` (88.8 ток/с), `pro` — самая медленная (30.6 ток/с), т.е. у `pro` выше накладные расходы на ответ даже при меньшем объёме текста.

**Ресурсоёмкость и стоимость:** при одинаковом входе (26 токенов) расход полностью определяется длиной ответа. `flash` — самый экономный (93 токена, ¥0.000341). `pro` тратит почти столько же токенов (107) на такой же по сути ответ, но из-за цены ×3 по входу и ×3 по выходу стоит в ~3.6 раза дороже (¥0.001211). `vision-exp` — самый «прожорливый» по токенам (236, в 2.5 раза больше `flash`) из-за многословия, по цене ~2.9× от `flash` (¥0.000984).

## Вывод

На простой логической задачке при `temperature=0` и `thinking=disabled` **качество всех трёх моделей одинаковое** (корректная разгадка без ошибок), поэтому выбор определяется скоростью и ценой:

- **`deepseek-v4-flash` — оптимальна**: самый быстрый ответ (1.34 с) и минимальная стоимость (¥0.000341) при той же корректности.
- **`deepseek-v4-pro` не даёт выигрыша** на такой задаче: медленнее почти в 2 раза и дороже в ~3.6 раза при ответе той же сути и качества.
- **`deepseek-v4-flash-vision-exp` нецелесообразна для текстовых задач**: пользы от vision-модели нет, а многословие даёт наибольший расход токенов (236) и стоимость ~2.9× от `flash`.

Правило для ежедневных/лёгких запросов подтверждается: `flash` — лучший баланс скорость/цена; `pro` стоит привлекать только там, где реально нужно более глубокое рассуждение или качество старшей модели.
